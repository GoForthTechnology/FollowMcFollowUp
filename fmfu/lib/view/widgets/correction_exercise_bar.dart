import 'package:flutter/material.dart';

class CorrectionExerciseBar extends StatelessWidget {
  const CorrectionExerciseBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(children: [
      Text("Student: Parker Roth"),
    ]);
  }
}